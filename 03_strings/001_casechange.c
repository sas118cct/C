#include<stdio.h>
#include<string.h>
int main()
{
	char a[10];
	printf("Enter a string in uppercase:");
	scanf("%s",&a[10]);
	printf("The string in lowercase is:%s",  strlwr(a));
	return 0;
}
